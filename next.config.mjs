/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.thesagenext.com', 'snb.thesagenext.com'],
  },
  

};

export default nextConfig;
